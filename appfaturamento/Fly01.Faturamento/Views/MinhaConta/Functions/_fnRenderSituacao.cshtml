<script>
    function fnRenderSituacao(data, type, row, meta) {
        return createElem("span", { "class": "left no-margin badge cool " + `${(row.situacao == 'PENDENTE') ? "blue" : "green"}` }, row.situacao).outerHTML;
    };

</script>