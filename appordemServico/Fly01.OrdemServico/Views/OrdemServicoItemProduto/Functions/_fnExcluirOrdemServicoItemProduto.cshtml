<script>
    function fnExcluirOrdemServicoItemProduto(idRecord) {
        $$$.confirm("Confirmar Exclusão", "Deseja realmente excluir este registro?", "fnExcluirOrdemServicoItemProdutoConfirmed(\"" + idRecord + "\")");
    }
    function fnExcluirOrdemServicoItemProdutoConfirmed(idRecord) {
        $('body').removeClass('loaded');
        if (idRecord) {
            $.ajax({
                type: "POST",
                url: "@Url.Action("Delete", "OrdemServicoItemProduto")",
                data: { id: idRecord },
                success: function (data) {
                    $('body').addClass('loaded');
                    if (data.success) {
                        $$$.toast("Excluído com sucesso.", "success");
                        $$$._.dtOrdemServicoItemProdutos.refresh();
                    }
                    else {
                        $$$.fn.submitErrorHandler(null, data.message);
                    }
                },
                error: function (jXHR, textStatus, errorThrown) {
                    $('body').addClass('loaded');
                    $$$.toast(textStatus, "error");
                }
            });
        }
    }
</script>