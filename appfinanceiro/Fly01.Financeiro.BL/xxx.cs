using Fly01.Core.Base;
using Fly01.Core.Entities.Domains.Commons;
using Fly01.Core.ServiceBus;
using System;
using System.Collections.Generic;

namespace Fly01.Financeiro.BL
{
    public class xxx
    {
        //public async void Teste()
        //{
        //    var loteRegistros = new List<string>();

        //    using (var uow = new UnitOfWork(new ContextInitialize() { AppUser = "SASA", PlataformaUrl = "SASA" }))
        //    {
        //        foreach (var item in loteRegistros)
        //        {
        //            var routingKey = item;

        //            switch (routingKey)
        //            {
        //                case "ContaPagar":
        //                    {
        //                        //var conta = new ContaPagar();
        //                        //uow.ContaPagarBL.metotodopersistemessageespecial(PersistMessage(new ContaPagar(), Core.ServiceBus.RabbitConfig.EnHttpVerb.POST, true);
        //                        break;
        //                    }
        //                case "ContaReceber":
        //                    uow.ContaPagarBL.PersistMessage(new ContaPagar(), Core.ServiceBus.RabbitConfig.EnHttpVerb.POST, true);
        //                    break;
        //                default:
        //                    throw new ArgumentException("Fila não implementada");
        //            }
        //            await uow.Save();

        //        }
        //    }
        //}
    }
}
