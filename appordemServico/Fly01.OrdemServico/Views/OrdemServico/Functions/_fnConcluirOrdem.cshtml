<script>
    function fnConcluirOrdem(idRecord) {
        $$$.confirm("Confirmar Encerramento", "Deseja realmente concluir esta ordem? Não será mais possível editar/excluir", "fnConcluirOrdemConfirmed(\"" + idRecord + "\")");
    }
    function fnConcluirOrdemConfirmed(idRecord) {
        $$$.loading.start();
        $.ajax({
            type: "POST",
            url: "@Url.Action("ConcluirOrdem", "OrdemServico")",
            dataType: 'json',
            data: { id: idRecord, faturar: false },
            success: function (data) {
                $$$.loading.stop();
                if (data.success) {
                    $$$.toast("Ordem concluída com sucesso!", "success");
                    $$$._.fly01dt.refresh();
                }
                else {
                    $$$.fn.submitErrorHandler(null, data.message);
                }
            },
            error: function (jXHR, textStatus, errorThrown) {
                $$$.loading.stop();
                $$$.toast(textStatus, "error");
            }
        });
    }
</script>