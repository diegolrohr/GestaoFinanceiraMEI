<script>
    function fnTotais() {
        $.ajax({
            url: '@Url.Action("Totais", "PosicaoAtual")',
            type: "GET",
            success: function (data) {
                if (data.estoqueTotal == 0)
                {
                    data.estoqueTotal = "0"
                }
                $$$._.fly01cardEstoque.info(data.estoqueTotal);
                $$$._.fly01cardCustos.info(data.custoTotal);
                $$$._.fly01cardVendas.info(data.vendaTotal);
            },
            error: function (jXHR, textStatus, errorThrown) {
                $$$.toast(data.message, 'error');
            }
        });
    }
</script>