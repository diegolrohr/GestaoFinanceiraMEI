<script>
    function fnCancelarOrcamento() {
        var idRecord = $('form#fly01frm input#id').val();
        if (onCancelMustDelete) {
            $$$.loading.start();
            $.ajax({
                type: "POST",
                url: "@Url.Action("Delete", "Orcamento")",
                data: { id: idRecord },
                success: function (data) {
                    $$$.loading.stop();
                    if (data.success) {
                        $$$.toast("Cancelado com sucesso.", "success");
                        $$$.go("@Url.Action("List", "OrdemCompra")");
                    }
                    else {
                        $$$.toast(data.message, "error");
                    }
                },
                error: function (jXHR, textStatus, errorThrown) {
                    $$$.loading.stop();
                    $$$.toast(textStatus, "error");
                }
            });
        }
        else {
            $$$.go("@Url.Action("List", "OrdemCompra")");
        }
    }
</script>