<script>
    @Html.Partial("Functions/_fnMain");

    function fnSalvarNovoRecebimento() {
        if (!$('form#fly01frmNovoReceb').valid()) {
            return false;
        }
        else {
            $('body').removeClass('loaded');
            var jsonData = $("form#fly01frmNovoReceb").serializeObject();

            $.ajax({
                contentType: 'application/json; charset=utf-8',
                dataType: 'json',
                type: 'POST',
                url: '@Url.Action("NovoRecebimento","Extrato")',
                data: JSON.stringify({ 'recebimento': jsonData}),
                success: function success(data) {
                    if (data.success) {
                        var strMsg = "Recebimento cadastrado com sucesso.";
                        if (data.message !== "") strMsg = data.message;
                        fly01.toast(strMsg, "success");
                        fnAtualizar();
                        fly01._.fly01frmNovoReceb.close();
                    } else {
                        fly01.toast(data.message, "error");
                    }
                    $('body').addClass('loaded');
                },
                error: function error(jXHR, textStatus) {
                    fly01.toast(textStatus, "error");
                    $('body').addClass('loaded');
                }
            });
        }
    }
</script>