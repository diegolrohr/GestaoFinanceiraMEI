<script>
    function fnExcluirOrdemVendaServico(idRecord) {
        $$$.confirm("Confirmar Exclusão", "Deseja realmente excluir este registro?", "fnExcluirOrdemVendaServicoConfirmed(\"" + idRecord + "\")");
    }
    function fnExcluirOrdemVendaServicoConfirmed(idRecord) {
        $('body').removeClass('loaded');
        if (idRecord) {
            $.ajax({
                type: "POST",
                url: "@Url.Action("Delete", "OrdemVendaServico")",
                data: { id: idRecord },
                success: function (data) {
                    $('body').addClass('loaded');
                    if (data.success) {
                        $$$.toast("Excluído com sucesso.", "success");
                        $$$._.dtOrdemVendaServicos.refresh();
                    }
                    else {
                        $$$.fn.submitErrorHandler(null, data.message);
                    }
                },
                error: function (jXHR, textStatus, errorThrown) {
                    $('body').addClass('loaded');
                    $$$.toast(textStatus, "error");
                }
            });
        }
    }
</script>