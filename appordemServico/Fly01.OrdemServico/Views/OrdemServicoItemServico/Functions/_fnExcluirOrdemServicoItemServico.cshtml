<script>
    function fnExcluirOrdemServicoItemServico(idRecord) {
        fly01.confirm("Confirmar Exclusão", "Deseja realmente excluir este registro?", "fnExcluirOrdemServicoItemServicoConfirmed(\"" + idRecord + "\")");
    }
    function fnExcluirOrdemServicoItemServicoConfirmed(idRecord) {
        $('body').removeClass('loaded');
        if (idRecord) {
            $.ajax({
                type: "POST",
                url: "@Url.Action("Delete", "OrdemServicoItemServico")",
                data: { id: idRecord },
                success: function (data) {
                    $('body').addClass('loaded');
                    if (data.success) {
                        fly01.toast("Excluído com sucesso.", "success");
                        fly01._.dtOrdemServicoItemServicos.refresh();
                    }
                    else {
                        fly01.fn.submitErrorHandler(null, data.message);
                    }
                },
                error: function (jXHR, textStatus, errorThrown) {
                    $('body').addClass('loaded');
                    fly01.toast(textStatus, "error");
                }
            });
        }
    }
</script>