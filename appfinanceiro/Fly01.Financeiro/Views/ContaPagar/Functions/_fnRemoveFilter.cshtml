<script>
    function fnRemoveFilter() {
        $$$.go("@Url.Action("ListContaPagar", "ContaPagar")" + "?action=GridLoadNoFilter");
        $$$._.fly01dt.refresh();
        $("#dataInical").val("");
        $("#dataFinal").val("");

        $("input#dataVencimento").on("change", function () {
            if ($("label[for='dataVencimento']").attr("class") === "truncate") {
                console.log("sssss");
            }
        });
    }
</script>