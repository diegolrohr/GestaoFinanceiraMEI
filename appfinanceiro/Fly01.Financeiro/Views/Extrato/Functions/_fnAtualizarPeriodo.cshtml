<script>
    function fnAtualizarPeriodo(days) {
        var today = new Date();
        var initialDate = new Date();
        initialDate.setDate(today.getDate() - parseInt(days));

        fly01._.dataFinal.set(today);
        fly01._.dataInicial.set(initialDate);

        fnAtualizar();
    }
</script>