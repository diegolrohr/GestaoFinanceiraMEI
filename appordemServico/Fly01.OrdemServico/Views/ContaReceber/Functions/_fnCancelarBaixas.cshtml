
<script>
    function fnCancelarBaixas(idRecord) {
        fly01.confirm("Confirmar Cancelamento", "Deseja realmente cancelar o/s pagamento/s deste título?", "fnCancelarBaixasConfirmed(\"" + idRecord + "\")");
    }
    function fnCancelarBaixasConfirmed(idRecord) {
        if (idRecord) {
            $.ajax({
                type: "POST",
                url: "@Url.Action("CancelarBaixaTitulo")",
                data: { id: idRecord },
                success: function (data) {
                    if (data.success) {
                        fly01.toast("Pagamento/s cancelado/s com sucesso.", 'success');
                        $("#fly01dt").DataTable().ajax.reload(null, false);
                    }
                    else {
                        fly01.toast(data.message, 'error');
                    }
                },
                error: function (jXHR, textStatus) {
                    fly01.toast(textStatus, 'error');
                }
            });
        }
    }

</script>