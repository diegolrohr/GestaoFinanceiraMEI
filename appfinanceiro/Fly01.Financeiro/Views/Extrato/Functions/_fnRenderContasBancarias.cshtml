<script>
    function fnRenderContasBancarias(data, type, full, meta) {
         return createElem("div", { "class": "tooltipped", "data-position": "top", "data-tooltip": "Representação: " + full.progressSaldo + "%", "data-conta-id": full.contaBancariaId }, [
            createElem("span", { "class": "left" }, full.contaBancariaDescricao),
             createElem("strong", { "class": "right primary-color-text" }, full.contaSaldo),
            createElem("div", { "class": "progress" }, [
                createElem("div", { "class": "determinate", "style": "width: " + full.progressSaldo + "%" })
            ])
        ]).outerHTML;
    }
</script>