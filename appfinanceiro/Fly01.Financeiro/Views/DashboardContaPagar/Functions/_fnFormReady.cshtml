<script>
    function __configsChart() {
        //var simboloMonetario = " R$ ";
        //fly01._.chartStatus.options.tooltips.callbacks.label = function (tooltipItems, data) {
        //    var formatedValue = __formatMoney(tooltipItems.yLabel);
        //    return simboloMonetario + formatedValue;
        //}
        //fly01._.chartStatus.update();

        //fly01._.chartPagamento.options.tooltips.callbacks.label = function (tooltipItems, data) {
        //    var formatedValue = __formatMoney(tooltipItems.yLabel);
        //    return simboloMonetario + formatedValue;
        //}
        //fly01._.chartPagamento.update();

        //fly01._.chartCategoria.options.tooltips.callbacks.label = function (tooltipItems, data) {
        //    var formatedValue = __formatMoney(tooltipItems.yLabel);
        //    return simboloMonetario + formatedValue;
        //}
        //fly01._.chartCategoria.update();
    }
    function refreshDash() {
        fly01._.chartCategoria.refresh();
        fly01._.chartPagamento.refresh();
        fly01._.chartStatus.refresh();
        fly01._.dtDashCP.refresh();
    }
    function fnFormReady() {
        let date = fly01._.mesPicker.pickamonth("picker").get("select");
        var firstDay = new Date(date.year, date.month, 1);
        $("#dataInicial").val(moment(firstDay).format('YYYY-MM-DD'));
        __configsChart();
        fly01._.dtDashCP.refresh();
    }
</script>