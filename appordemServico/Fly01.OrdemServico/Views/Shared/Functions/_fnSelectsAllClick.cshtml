<script>
    function fnSelectsAllClick(btnValue) {
        switch (btnValue) {
            case "selectAll":
                fly01._.dtContasExistentes.rows().select();
                break;
            case "deselectAll":
                fly01._.dtContasExistentes.rows().deselect();
                break;
            default:
                break;
        }
    }
</script>