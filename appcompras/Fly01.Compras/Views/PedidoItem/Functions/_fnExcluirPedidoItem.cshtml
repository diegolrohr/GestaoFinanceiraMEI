<script>
    function fnExcluirPedidoItem(idRecord) {
        fly01.modal.confirm("Confirmar Exclusão", "Deseja realmente excluir este registro?", "fnExcluirPedidoItemConfirmed(\"" + idRecord + "\")");
    }
    function fnExcluirPedidoItemConfirmed(idRecord) {
        $('body').removeClass('loaded');
        if (idRecord) {
            $.ajax({
                type: "POST",
                url: "@Url.Action("Delete","PedidoItem")",
                data: { id: idRecord },
                success: function (data) {
                    $('body').addClass('loaded');
                    if (data.success) {
                        fly01.toast("Excluído com sucesso.", "success");
                        fly01._.dtPedidoItens.refresh();
                    }
                    else {
                        fly01.toast(data.message, "error");
                    }
                },
                error: function (jXHR, textStatus, errorThrown) {
                    $('body').addClass('loaded');
                    fly01.toast(textStatus, "error");
                }
            });
        }
    }
</script>