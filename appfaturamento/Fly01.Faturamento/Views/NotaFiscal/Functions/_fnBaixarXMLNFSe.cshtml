<script>
    function fnBaixarXMLNFSe(idRecord) {
        fly01.loading.start();
        $.ajax({
            url: '@Url.Action("BaixarXML", "NFSe")?id=' + idRecord,
            type: "GET"
        })
        .done(function (data) {
            fly01.loading.stop();
            if (data.message) {
                fly01.fns.submitErrorHandler(null, data.message);
            } else {
                location.href = data.downloadAddress;
            }
        })
        .fail(function (jqxhr, textStatus, error) {
            var err = 'Ocorreu um erro na requisição: ' + textStatus + ", " + error;
            fly01.toast(err, "error");
            fly01.loading.stop();
        });
    }
</script>